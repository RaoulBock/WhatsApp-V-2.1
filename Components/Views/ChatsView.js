import { StyleSheet, Text, View } from "react-native";
import React from "react";

const ChatsView = () => {
  return (
    <View style={styles.outline}>
      <Text style={styles.title}>ChatsView</Text>
    </View>
  );
};

export default ChatsView;

const styles = StyleSheet.create({});
